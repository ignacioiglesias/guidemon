#!/usr/bin/env node
var debug = require('debug')('guidemon');
var http = require('../app');
var port = process.env.PORT || 3000;

http.listen(port, function() {
    console.log('Guidemon listening on ', port);
});
